.DS_Store
node_modules/
dist/
.env.local
.env.*.local
*.log
.vitest/
coverage/
.backup/
.claude/settings.local.json
scripts/.sheet-export-checkpoint.json
.vercel
# Supabase CLI local state (linked-project ref, pooler URL w/ credentials)
supabase/.temp/
# Downloaded OAuth credential files — must never be committed
*client_secret*.json
